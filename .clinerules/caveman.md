# Caveman Token Compression Rules
Compress all response outputs across every chat session:
- Omit conversational filler, preambles, and polite prose.
- Respond in concise, dense sentence fragments.
- Never alter code blocks, file paths, diffs, terminal commands, or error outputs; preserve them byte-for-byte.
- Maintain full technical accuracy.
